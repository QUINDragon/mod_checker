# Bug 记录

> 记录本地操作中发现的 bug。格式：操作 → 现象

---

## 2026-06-05

### #1 — 点击 MOD 报 HTTP 401
- **操作**: 双击 exe → GUI 扫描完成 → 点击任意 MOD
- **现象**: 详情页报错 `HTTP 401`，N网 API 未配置
- **状态**: ✅ 已修复 — 无 Key 时跳过 API 调用 + 弹窗提示输入

### #2 — `recs` 未定义
- **操作**: 点击 MOD 查看详情
- **现象**: `ERROR 任务失败 [deep_scan_multiple]: name 'recs' is not defined`
- **原因**: `deep_scan.py` 中 `recs = []` 初始化语句被意外删除
- **状态**: ✅ 已修复 — 补回 `recs = []`

### #3 — GUI 启动闪退
- **操作**: 双击 ModChecker.exe
- **现象**: 窗口一闪而过，程序退出
- **原因**: `gui/app.py` 中 Tkinter `style.map` 语法不兼容，notebook 初始化代码混入 `_append_log` 方法
- **状态**: ✅ 已修复 — 修正样式配置 + 重构布局代码

### #4 — exe 不显示版本名
- **操作**: 扫描完成后查看 MOD 列表
- **现象**: 版本号列没有数据显示
- **原因**: Treeview 列配置只设了 type/version 但数据填充不匹配
- **状态**: ✅ 已修复 — 改为 name/version 两列并正确对齐

### #5 — CMD 窗口弹出
- **操作**: 双击 exe
- **现象**: 同时出现黑色 CMD 窗口和 GUI 窗口，日志输出到 CMD
- **状态**: ✅ 已修复 — `--noconsole` 打包 + 日志重定向到 GUI 面板

### #6 — 诊断/修复结果不显示
- **操作**: 点击「诊断」或「修复」
- **现象**: 日志输出到已关闭的 CMD，GUI 面板空白
- **状态**: ✅ 已修复 — 全局日志重定向到 GUI `_redirect_log()`

---

---

## 测试摘要 (2026-06-05)

| 测试项 | 结果 |
|------|------|
| 扫描 MOD | ✅ |
| 点击查看详情 | ✅ |
| 诊断 (12 actions) | ✅ |
| 修复预览 (dry-run) | ✅ |
| API Key 设置 | ✅ |
| 调试模式 | ✅ |
| 单元测试 (29) | ✅ |
| 模块导入 (26) | ✅

## 模板

### #N — 标题
- **操作**: 具体操作步骤
- **现象**: 错误信息 / 截图描述
- **状态**: 🔍 待修复 / ✅ 已修复 / ⚠️ 暂不处理
